// Hamburger Menu Materialize
document.addEventListener('DOMContentLoaded', function() {
    const $sidenav = document.querySelectorAll('.sidenav');
    const instances = M.Sidenav.init($sidenav);
    // $(".dropdown-trigger").dropdown();
});

